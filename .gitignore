# Built binary (produced by `go build` / install.sh)
/agent-local

# Go build/test artifacts
*.test
*.out
/dist/

# Local runtime state lives in ~/.agent-local, never in the repo
/sites/
/dumps/
/logs/
/run/

# Planning artifacts: working notes for a change, not part of the product
/goals/

# macOS
.DS_Store
site/node_modules/
